import { IGenerateResult } from "custom-functions-metadata";
import { Compiler } from "webpack";
declare type Options = {
    input: string;
    output: string;
};
declare class CustomFunctionsMetadataPlugin {
    private options;
    constructor(options: Options);
    static generateResults: Record<string, IGenerateResult>;
    apply(compiler: Compiler): void;
}
export = CustomFunctionsMetadataPlugin;
