import { FxError, LogProvider, M365TokenProvider, Result, TelemetryReporter, UserInteraction, Void } from "@microsoft/teamsfx-api";
export interface BotDebugArgs {
    botId?: string;
    botPassword?: string;
    botMessagingEndpoint?: string;
}
export declare class BotDebugHandler {
    private readonly projectPath;
    private args;
    private readonly m365TokenProvider;
    private readonly logger?;
    private readonly telemetry?;
    private readonly ui?;
    constructor(projectPath: string, args: BotDebugArgs, m365TokenProvider: M365TokenProvider, logger?: LogProvider, telemetry?: TelemetryReporter, ui?: UserInteraction);
    setUp(): Promise<Result<Void, FxError>>;
    private setEnvs;
    private checkArgs;
}
//# sourceMappingURL=bot.d.ts.map