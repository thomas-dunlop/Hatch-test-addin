import { FxError, LogProvider, M365TokenProvider, Result, TelemetryReporter, UserInteraction, Void } from "@microsoft/teamsfx-api";
export interface SSODebugArgs {
    objectId?: string;
    clientId?: string;
    clientSecret?: string;
    accessAsUserScopeId?: string;
}
export declare class SSODebugHandler {
    private readonly projectPath;
    private args;
    private readonly m365TokenProvider;
    private readonly logger?;
    private readonly telemetry?;
    private readonly ui?;
    constructor(projectPath: string, args: SSODebugArgs, m365TokenProvider: M365TokenProvider, logger?: LogProvider, telemetry?: TelemetryReporter, ui?: UserInteraction);
    setUp(): Promise<Result<Void, FxError>>;
    private setEnvs;
    private checkArgs;
    private constructPluginContext;
}
//# sourceMappingURL=sso.d.ts.map