import { FxError, LogProvider, M365TokenProvider, Result, TelemetryReporter, UserInteraction, Void } from "@microsoft/teamsfx-api";
export interface AppManifestDebugArgs {
    manifestPackagePath?: string;
}
export declare class AppManifestDebugHandler {
    private readonly projectPath;
    private args;
    private readonly m365TokenProvider;
    private readonly logger?;
    private readonly telemetry?;
    private readonly ui?;
    constructor(projectPath: string, args: AppManifestDebugArgs, m365TokenProvider: M365TokenProvider, logger?: LogProvider, telemetry?: TelemetryReporter, ui?: UserInteraction);
    prepare(): Promise<Result<Void, FxError>>;
    private checkArgs;
}
//# sourceMappingURL=appManifest.d.ts.map