/// <reference types="node" />
export declare class FuncHostedDeployMgr {
    private readonly workingDir;
    private readonly deploymentDir;
    private readonly deploymentInfoFile;
    private readonly deploymentZipCacheFile;
    private readonly envName;
    constructor(workingDir: string, envName: string);
    getLastDeployTime(): Promise<Date>;
    needsToRedeploy(): Promise<boolean>;
    saveDeploymentInfo(zipContent: Buffer, deployTime: Date): Promise<void>;
    private loadLastDeploymentZipCache;
    private removeLegacyFileInZip;
    zipAFolder(rules: string[]): Promise<Buffer>;
    private static prepareIgnore;
    getIgnoreRules(fileName: string): Promise<string[]>;
}
//# sourceMappingURL=deployMgr.d.ts.map