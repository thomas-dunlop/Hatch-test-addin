"use strict";
// Copyright (c) Microsoft Corporation.
// Licensed under the MIT license.
Object.defineProperty(exports, "__esModule", { value: true });
exports.ResourceNameFactory = void 0;
const constants_1 = require("../constants");
const errors_1 = require("../errors");
const strings_1 = require("../resources/strings");
class ResourceNameFactory {
    static createCommonName(suffix, appName, limit) {
        if (!appName) {
            throw new errors_1.SomethingMissingError(strings_1.CommonStrings.SHORT_APP_NAME);
        }
        const normalizedAppName = appName.replace(constants_1.RegularExprs.CHARS_TO_BE_SKIPPED, "").toLowerCase();
        const lowerAlias = constants_1.Alias.TEAMS_BOT_PLUGIN.toLowerCase();
        let candidate = `${normalizedAppName}${lowerAlias}${suffix}`;
        if (limit && candidate.length > limit) {
            candidate = candidate.substr(candidate.length - limit);
        }
        return candidate;
    }
}
exports.ResourceNameFactory = ResourceNameFactory;
//# sourceMappingURL=resourceNameFactory.js.map