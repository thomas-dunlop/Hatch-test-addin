import { IProgressHandler, UserInteraction } from "@microsoft/teamsfx-api";
export declare class StepHelper {
    progressHandler?: IProgressHandler;
    message: string;
    title: string;
    constructor(title: string);
    start(entireSteps: number, ui?: UserInteraction): Promise<void>;
    forward(message: string): Promise<void>;
    end(success: boolean): Promise<void>;
}
//# sourceMappingURL=step-helper.d.ts.map